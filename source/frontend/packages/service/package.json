{
  "name": "service",
  "version": "0.1.0",
  "scripts": {
    "test": "jest",
    "lint": "eslint .",
    "lint:fix": "eslint --fix .",
    "fix": "prettier --write . && eslint . --fix",
    "check": "prettier --check . && eslint . && tsc --noEmit"
  },
  "dependencies": {
    "entities": "workspace:*",
    "uuid": "^11.0.3"
  }
}
