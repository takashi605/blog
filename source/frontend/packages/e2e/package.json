{
  "name": "e2e",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "e2e-test": "pnpm exec bddgen && pnpm exec playwright test",
    "e2e-ui": "pnpm exec bddgen && pnpm exec playwright test --ui",
    "e2e-report": "pnpm exec playwright show-report tests/report --host 0.0.0.0 --port 9323",
    "lint": "eslint .",
    "lint:fix": "eslint --fix ."
  },
  "dependencies": {
    "shared-interface-adapter": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.46.1",
    "@types/node": "^22.5.1",
    "playwright-bdd": "^6.6.0"
  }
}
