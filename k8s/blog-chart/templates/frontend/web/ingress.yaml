apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "web.appname" . }}
  labels:
    app.kubernetes.io/name: {{ include "web.appname" . }}
    {{- include "blog-chart.labels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: {{ .Values.web.ingress.rewriteTarget }}
spec:
  rules:
    - {{- if .Values.web.ingress.host }}
      host: {{ .Values.web.ingress.host }}
      {{- end }}
      http:
        paths:
        - pathType: {{ .Values.web.ingress.pathType }}
          path: {{ .Values.web.ingress.path }}
          backend:
            service:
              name: {{ include "web.appname" . }}
              port:
                number: {{ .Values.web.service.port }}
