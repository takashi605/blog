apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "admin.appname" . }}
  labels:
    app.kubernetes.io/name: {{ include "admin.appname" . }}
    {{- include "blog-chart.labels" . | nindent 4 }}
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: {{ .Values.admin.ingress.rewriteTarget }}
spec:
  rules:
    - {{- if .Values.admin.ingress.host }}
      host: {{ .Values.admin.ingress.host }}
      {{- end }}
      http:
        paths:
        - pathType: {{ .Values.admin.ingress.pathType }}
          path: {{ .Values.admin.ingress.path }}
          backend:
            service:
              name: {{ include "admin.appname" . }}
              port:
                number: {{ .Values.admin.service.port }}
