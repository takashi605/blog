apiVersion: v1
kind: Service
metadata:
  name: {{ include "blog-admin.appname" . }}
  labels:
    app.kubernetes.io/name: {{ include "blog-admin.appname" . }}
    {{- include "blog-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.blog-admin.service.type }}
  ports:
    - port: {{ .Values.blog-admin.service.port }}
      targetPort: {{ .Values.blog-admin.service.targetPort }}
      protocol: TCP
      {{- if eq .Values.blog-admin.service.type "NodePort" }}
      nodePort: {{ .Values.blog-admin.service.nodePort }}
      {{- end }}
  selector:
    app.kubernetes.io/name: {{ include "blog-admin.appname" . }}
  {{- if eq .Values.blog-admin.service.type "LoadBalancer" }}
  loadBalancerIP: {{ .Values.blog-admin.service.loadBalancerIP }}
  externalTrafficPolicy: {{ .Values.blog-admin.service.externalTrafficPolicy }}
  {{- end }}
